using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RotateBuilding : MonoBehaviour
{
    void Update()
    {

        // transform.Rotate(transform.up, Time.deltaTime * speed);
        //building.transform.Rotate(building.transform.up, Time.deltaTime * speed);
    }
}
